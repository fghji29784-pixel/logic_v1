# SDM_logic — 리튬이차전지 자가방전 단시간 판정 프로젝트

## 프로젝트 목표

- **핵심 목표**: 3일간의 전압강하량(OCV drop)을 이용해 리튬이차전지 저전압 불량셀을 **15분 이내**에 판정
- **최종 목표**: **10분 이내** 판정 (생산성 극대화)
- **불량 정의**: 양품 대비 자가방전량이 비정상적으로 큰 셀 — 주 원인은 공정 내 금속이물(Fe, Cu, Ni 등) 유입 → 덴드라이트(dendrite) 성장 → 내부 미세 단락

---

## 사용 장비: Keysight BT2152B Self-Discharge Analyzer

### 장비 개요

| 항목 | 사양 |
|---|---|
| 모델 | BT2152B Self-Discharge Analyzer |
| 전압 범위 | 0.5 ~ 4.5 V |
| 채널 수 | 32채널 (병렬 측정) |
| 전류 측정 정밀도 | ±(0.30% + 250 nA) |
| 전압 안정도 | ±3 µVpk |
| OCV 매칭 정밀도 | ±5 µV 이내 |
| DC 소스 드리프트 | < 10 µV / 24시간 |

### 장비가 하는 일

BT2152B는 **포텐시오스태틱(Potentiostatic) 방식**으로 셀의 자가방전 전류를 직접 측정한다.  
기존 OCV 차이법(delta-OCV)이 수 주~수 개월이 걸리는 것과 달리, 이 장비는 수십 분~수 시간 안에 µA 수준의 자가방전 전류를 직접 읽어낸다.

---

## 측정 원리: 포텐시오스태틱(Potentiostatic) 방법

### 등가 회로 모델 (논문 R-Smith 2020, 2023)

```
              Rwire          Rout (0.1 ~ 10 Ω)
[DC Source] ──┤├── [µA meter] ──┤├──────────────────────┐
(Vext ≈ OCV)                                            │
                                              ┌─────────┤
                                              │  Rser   │  ← 내부 직렬 저항 (mΩ)
                                              ├─────────┤
                                              │ Ceff ║ RSD │  ← 셀 유효 커패시턴스 (kF~MF)
                                              │         │     RSD = 자가방전 저항 (kΩ~MΩ)
                                              └─────────┘
```

- **IMeas** (측정 전류) = 자가방전 전류 ISD로 수렴
- **RSD** = Vcell / ISD 로 계산 — 양품: ~MΩ 범위, 불량: kΩ 이하
- **Rwire** (배선 저항)는 캘리브레이션으로 제거 (측정 plane 보정)

### 측정 시간 상수

```
τ = (Rout + Rser + Rwire) × Ceff
```

| Rout | 시간상수 τ | 특성 |
|---|---|---|
| 1 Ω | ~4시간 | 빠른 수렴, 노이즈 큼 |
| 2 Ω | ~7시간 | 중간 |
| 3 Ω | ~11시간 | 느린 수렴, 노이즈 작음 (RMS error < 0.3 µA) |

**실측 확인**: 실제 장비 운용 시 τ ≈ 6시간(360분). 15분은 t/τ ≈ 0.042로 완전 선형 구간에 해당.

### 측정 절차

1. **OCV 측정**: 셀의 개방회로전압 측정
2. **전압 매칭**: DC 전원을 셀 OCV에 ±5 µV 이내로 정밀 매칭
3. **전류 측정**: IMeas = ISD + A·exp(-t/τ) 형태로 지수 수렴 (단, 15분은 선형 구간)
4. **ISD 추출**: 충분히 수렴하면 직독 (6시간 이상 필요)
5. **판정**: ISD vs. 임계값 비교 → Pass/Fail

---

## 핵심 물리 현상

### 자가방전 발생 메커니즘

```
정상 셀:  SEI 성장에 의한 아주 느린 Li 소모 → 3~6 µA (양품 기준)
불량 셀:  금속이물(Fe/Cu/Ni) 유입
              ↓
         덴드라이트(dendrite) 성장 — 분리막 관통
              ↓
         내부 미세 단락(internal micro short)
              ↓
         비정상적 자가방전 전류: 150 µA 이상 (약 25~50배 차이)
```

### 자가방전 전류 기준값 (논문 실측)

| 셀 상태 | ISD 범위 | RSD 범위 |
|---|---|---|
| 양품 (정상) | 3 ~ 6 µA (23°C, 30% SOC) | ~0.8 MΩ |
| 양품 (고온) | 97 µA (40°C, 30% SOC) | 감소 |
| 불량 (미세단락) | 150 µA 이상 | kΩ 이하 |
| 측정 노이즈 floor | 0.1 ~ 0.25 µA | — |

### 자가방전의 물리화학적 원인 (Arrhenius 분석)

활성화에너지 **Eact = 0.94 ± 0.14 eV** → 화학적 부반응(SEI 성장, 전해질 분해)이 주 원인.  
물리적 결함(덴드라이트 단락)의 경우 Eact 값이 달라지므로, Eact 변화로 결함 메커니즘 구분 가능.

```
ISD = J · i1c · exp(α·Eact / R·T) + QSEI
```

---

## 온도 영향: TCV (Temperature Coefficient of Voltage)

### TCV vs SOC 특성 (실측)

| SOC | TCV |
|---|---|
| 0% | -320 µV/K |
| **~30%** | **≈ 0 µV/K (최소점)** |
| 40% | +180 µV/K |
| 70%~80% | 양의 값, 더 커짐 |

> **SOC 30% 부근이 TCV ≈ 0인 최적 측정 포인트.**  
> 온도 변동의 영향을 받지 않으므로 단시간 측정에서 신호 품질(SNR) 극대화.

### Arrhenius 온도 보정

```
ISD(T2) = ISD(T1) · exp[ (Eact/R) · (1/T1 - 1/T2) ]
```

온도 몇 도 차이만으로도 ISD가 수% 변화 → 트레이 내 온도 불균일은 반드시 보정 필요.

---

## 실측 데이터 분석 (SDM 문제.jpg)

### 측정 화면 구성 (8개 패널)

| 패널 | 핵심 관찰 |
|---|---|
| SDM 그래프 | **빨간 선 = 불량셀** — 다른 셀 대비 전류가 수십 배 높음 |
| 전압 그래프 | 대부분 밀집, 일부 컬러 셀이 상하 이탈 |
| 온도 그래프 | 개별 셀 온도 파동(노이즈) 존재 |
| dOCV-SDM 산점도 | **두 방법의 상관관계 검증 — 실제로 잘 일치** |
| SDM 히트맵 | **노란색 셀 1개 = 명확한 불량셀 위치 특정 가능** |
| 온도 히트맵 | **⚠ 중앙 고온, 가장자리 저온 — 공간적 온도 불균일 심각** |
| dOCV 히트맵 | SDM 히트맵과 동일한 불량셀 위치 확인 (일치성 검증) |
| Rwiring 히트맵 | **⚠ 특정 셀의 Rwiring 이상 높음 — 접촉 불량 문제** |

### 식별된 주요 문제점

**문제 1. 온도 불균일**: 중앙 고온/가장자리 저온. TCV ≈ 180 µV/K이면 1°C 차이 → OCV 180 µV 오차 → 측정 전류 직접 영향.

**문제 2. Rwiring 이상**: 특정 채널 접촉저항 과도 → τ 증가 → 같은 시간에 더 낮은 전류 측정 → 단시간 판정 정확도 저하.

---

## 선형 구간의 물리적 해석 (핵심 발견)

### 실측 조건

- **τ ≈ 6시간(360분)** — 실제 장비에서 전류가 평형에 도달하는 시간
- **t = 15분** → t/τ ≈ 0.042 → **완전 선형 구간**

### 선형 근사식

```
I(t) = ISD + (I₀ - ISD) · exp(-t/τ)

t << τ 이면: exp(-t/τ) ≈ 1 - t/τ

I₀ ≈ 0 이면:
I(t) ≈ ISD · (t/τ)    ← 핵심 관계식
```

### 함의

1. **15분 측정값 = ISD × (t/τ)** — ISD 자체가 아니라 ISD에 비례하는 값
2. **양품/불량 비율은 선형 구간에서도 보존**됨 → 판정 가능
   ```
   양품: ISD ≈ 5 µA  → I(15min) ≈ 5 × (15/360) = 0.21 µA
   불량: ISD ≈ 150 µA → I(15min) ≈ 150 × (15/360) = 6.25 µA
   비율: 30배 차이 유지
   ```
3. **지수 피팅으로 ISD 역산 불가** — 선형 구간 데이터만으로는 수렴값 추정 신뢰 불가 (수렴의 4%만 관찰)
4. **회귀 독립변수의 물리적 의미** = τ 변동 보정

### 회귀 독립변수가 실제로 보정하는 대상

```
τ = (Rout + Rser + Rwire) × Ceff
```

| 독립변수 | 실제로 보정하는 τ 구성요소 |
|---|---|
| 초기 전압 (V_init) | Ceff (SOC → 셀 용량) |
| 최종 온도 (T_final) | Rser (온도 의존성) |
| 온도 변화량 (ΔT) | 측정 중 τ 변동 |
| 위치 더미 | Rwiring + 위치별 온도 → τ 공간 의존성 |
| 공정 OCV (OCV1~7) | 셀 이력에 따른 Ceff/Rser 개인차 |

→ **회귀 구조가 물리적으로 타당함을 확인.**

### 최단 판정 가능 시간의 이론적 하한

```
측정 노이즈 floor = 0.25 µA

양품 신호가 노이즈를 초과하는 최소 시간:
I_good(t) = 5 × t/360 > 0.25 µA
→ t > 18분

불량 신호:
I_bad(t) = 150 × t/360 > 0.25 µA
→ t > 0.6분  (거의 즉시)
```

**→ 불량셀은 매우 일찍부터 노이즈 위에 있고, 양품은 ~18분부터 신호가 노이즈를 벗어남.**  
단시간 판정의 원리는 불량/양품의 절대적 신호 크기 차이(30배)를 이용하는 것.

---

## ══════════════════════════════════════════
## 합의된 판정 로직 v1 (기준안)
## ══════════════════════════════════════════

### 데이터 흐름

```
[SDM 측정 데이터]          [공정 데이터]
  ↓                           ↓
Step 1: SDM 변수 추출    Step 2: 공정 변수 추출
         ↓                    ↓
              Step 3: 이상치 제거
                      ↓
              Step 4: 위치 더미화
                      ↓
              Step 5~7: 모델링 (옵션 선택)
                      ↓
              Step 8: 평가지표 계산
```

---

### Step 1. SDM 데이터 추출 (트레이 단위)

추출 변수:
- SDM N분 전류 측정값 (종속변수 후보, N = 10 or 15 or ... 선택)
- 초기 전압 (V_init)
- 최종 온도 (T_final)
- 온도 변화량 (ΔT = T_final - T_init)

> ※ 초기 온도, 최종 전압, 전압 변화량은 다중공선성 방지를 위해 제외

---

### Step 2. 공정 데이터 추출

**공정 데이터 파일**: dOCV 파일과 동일한 단일 파일 (CSV/Excel)  
**1행 = 컬럼명**, 이후 행 = 셀 데이터

**매칭 키**: `TRAY ID` + `CELL NO` (공정단계별로 여러 컬럼 존재 → 하나만 사용)
- Tray ID 대표 컬럼: `TRAY ID_OCV #01`
- Cell NO 대표 컬럼: `CELL NO_OCV #01`
- KSS 폴더명(= Tray ID) + 셀 번호(1~144) → 공정 데이터 행 1:1 매핑

**실제 컬럼명 매핑**:

| 변수 | 실제 컬럼명 |
|---|---|
| OCV1 | `OCV_OCV #01` |
| OCV2 | `OCV_OCV #02` |
| OCV3 | `OCV_OCV #03` |
| OCV4 | `OCV_OCV #04` |
| OCV7 | `OCV_OCV #07` |
| 1차 충전 종료 전압 | ⚠ 미확정 (추후 확인) |
| **dOCV (3일 기준, 검증용)** | **`Delta OCV_Delta OCV #07`** |
| 판정등급 (A/E 레이블) | `판정등급` |
| Cell ID | `Cell ID` |
| Lot ID | `Lot ID` |

> ※ OCV #05, #06도 파일 내 존재(`OCV_OCV #05`, `OCV_OCV #06`). 현재 모델엔 미포함 — VIF 확인 후 추가 여부 결정.

추출 변수:
- OCV1, OCV2, OCV3, OCV4, OCV7 (각 시점 공정 OCV)
- 1차 충전 종료 전압

> ※ 각 OCV는 에이징/충전 단계가 끼어 있어 완전 선형 종속은 아니나,  
>   상관관계가 높을 수 있음 → Step 5 모델링 후 VIF 확인 필요

---

### Step 3. 이상치 제거

- Rwiring 임계값 초과 채널 제거 또는 플래깅
- 공정 OCV vs SDM V_init 간 괴리가 큰 셀 제거

---

### Step 4. 셀 위치 더미화 (12×12 트레이 기준)

트레이 144개 셀을 중앙부터 한 겹씩 6개 레이어로 구분 후 **5개 더미 변수** 생성  
(Layer 1 = 가장 바깥쪽 가장자리 = reference 기준)

```
Layer 1 (가장자리, reference):
  1~12, 13,24, 25,36, 37,48, 49,60, 61,72, 73,84, 85,96,
  97,108, 109,120, 121,132, 133~144

Layer 2:
  14~23, 26,35, 38,47, 50,59, 62,71, 74,83, 86,95, 98,107,
  110,119, 122~131

Layer 3:
  27~34, 39,46, 51,58, 63,70, 75,82, 87,94, 99,106, 111~118

Layer 4:
  40~45, 52, 64, 69, 76, 81, 88, 93, 100~105
  (※ 57번 셀 레이어 귀속 확인 필요 — 원본 데이터에 57:6 기재, 오타 의심)

Layer 5:
  53~56, 65, 68, 77, 80, 89~92

Layer 6 (중앙 4개):
  66, 67, 78, 79
```

회귀 투입: dummy_L2, dummy_L3, dummy_L4, dummy_L5, dummy_L6 (5개)

---

### Step 5~7. 모델링 옵션

**종속변수**: SDM N분 측정값 (N은 10, 12, 15 등 선택 가능, 옵션별 별도 모델 학습)

**독립변수**:
- [SDM계열] V_init, T_final, ΔT, dummy_L2~L6
- [공정계열] OCV1, OCV2, OCV3, OCV4, OCV7, 1차충전종료전압

| 옵션 | 모델 | 변수 | 특징 |
|---|---|---|---|
| **옵션 1** | OLS (일반 회귀) | SDM계열만 | 기준선 |
| **옵션 2** | OLS | SDM + 공정계열 | 공정 데이터 기여도 확인 |
| **옵션 3** | Robust 회귀 | SDM계열만 | 이상치 내성 |
| **옵션 4** | Robust 회귀 | SDM + 공정계열 | 이상치 내성 + 공정 데이터 |
| **옵션 5** | Lasso + LOO-tray CV | SDM + 공정계열 | 다중공선성·과적합 동시 해결 |

**Robust 회귀 절차 (옵션 3, 4)**:
1. 1차 OLS 회귀 → 잔차 계산
2. 잔차 10th percentile 이하 / 90th percentile 이상 데이터 제외
3. 나머지 데이터로 다중회귀 재추정
4. 재추정 모델의 잔차 제곱의 역수를 가중치로 WLS(가중최소제곱) 최종 추정

**Lasso 절차 (옵션 5)**:
```
minimize Σ(I_measured - Xβ)² + λ·Σ|β|

λ: LOO-tray CV로 최적화
   → 한 트레이를 검증 세트로 순환, 나머지로 학습
   → 공정 OCV 변수 중 불필요한 것 자동 제거
```

---

### Step 6. 보정값 계산

회귀계수를 사용해 기준 조건으로 보정:

```
기준 조건: T = 25°C, ΔT = 0°C, V_init = 3590 mV, 위치 = Layer 1(reference)

I_corrected = I_measured
              - β_Vinit  × (V_init  - 3590)
              - β_Tfinal × (T_final - 25)
              - β_ΔT     × (ΔT     - 0)
              - Σ β_Li   × dummy_Li
              [- β_공정변수 × (공정변수 - 기준값)]  ← 옵션 2,4,5만 해당
```

---

### Step 7. 표준화

- **기준 집단**: Robust 잔차 10~90% 구간 내 데이터 (정상 셀 집단)
- 해당 집단의 μ, σ로 z-score 표준화
- → 불량셀이 많은 트레이에서도 기준값 오염 방지

---

### Step 8. 평가지표

**주요 지표:**
```
분리도 = (μ_불량 - μ_양품) / σ_양품   ← d-prime
```

**보조 지표:**
- ROC AUC
- 혼동행렬 (TP / FP / FN / TN)
- 보정값의 표준편차

**최단 판정 시간 탐색:**
```
for N in [5, 6, 7, ..., 15]:
    모델 학습 (종속변수 = I(t=N분))
    분리도(N) 계산

→ 분리도(N) ≥ 임계값인 최소 N = 실용 최단 판정 시간
```

---

## ══════════════════════════════════════════
## 개선 제안 v2 (검토 중)
## ══════════════════════════════════════════

> v1과 다른 부분만 기술. v1 구조 위에 적용 가능하며, 옵션으로 추가 비교 가능.

### 변경점: 종속변수를 단일값 → 기울기(slope)로

**이유**: t << τ인 선형 구간에서는 단일 시점값보다 0~N분 구간의 기울기가 더 강건.

```
I(t) ≈ ISD · t/τ  (선형 구간)

→ slope = ΔI/Δt ≈ ISD/τ  (N개 포인트 평균 → 순간 노이즈 억제)
```

**장점:**
- 단일 시점의 순간 노이즈 영향 제거
- N 변경 시 비교 용이 (기울기는 N에 덜 의존)
- 물리적으로 ISD/τ에 직접 비례 → 회귀 해석 더 명확

**단점:**
- 시계열 전체를 저장·처리해야 함 (데이터 용량 증가)
- 기존 소프트웨어(BT2155A) 출력 포맷 확인 필요

**v2 독립변수 구성 (변경 없음):**
- [SDM계열] V_init, T_final, ΔT, dummy_L2~L6
- [공정계열] OCV1~OCV4, OCV7, 1차충전종료전압

**v2 옵션 구성:**

| 옵션 | 모델 | 변수 | 종속변수 |
|---|---|---|---|
| v2-옵션 1 | OLS | SDM만 | slope(0~N분) |
| v2-옵션 2 | OLS | SDM + 공정 | slope(0~N분) |
| v2-옵션 3 | Robust | SDM만 | slope(0~N분) |
| v2-옵션 4 | Robust | SDM + 공정 | slope(0~N분) |
| v2-옵션 5 | Lasso + LOO-tray CV | SDM + 공정 | slope(0~N분) |

---

## 측정 환경 권장 조건

| 조건 | 권장 | 근거 |
|---|---|---|
| 온도 | 23 ± 0.5°C | TCV 노이즈 억제 |
| SOC | **30%** (TCV ≈ 0) | 온도 변동 영향 최소화 |
| Rest time | 24h 이상 | relaxation 안정화 |
| Rout | 1~2 Ω | τ 단축 |
| 샘플링 간격 | 10초 | R-Smith 2020 기준 |
| 단열 처리 | 셀 fixture 단열재 감싸기 | 온도 저역통과 필터 효과 |
| 온도 측정 | PT100 센서 직접 부착 | 채널별 보정용 |

---

## ══════════════════════════════════════════
## Raw 데이터 구조 및 통합 데이터프레임 설계
## ══════════════════════════════════════════

### 측정 1회당 생성 파일 목록

| 파일 | 개수 | 비고 |
|---|---|---|
| KSS 파일 | 5개 | raw — 설비별 전류·전압·Rwiring |
| TEMP_DATA | 1개 | raw — 전체 144셀 온도 |
| MEDIAN_CURRENT | 1개 | 파생 (KSS 합산 결과) — **무시** |
| MEDIAN_VOLTATE | 1개 | 파생 (KSS 합산 결과) — **무시** |
| TEST_DATA, VITT_DATA | 각 1개 | 파생 — **무시** |

---

### KSS 파일 구조

**파일명 형식**:
```
20260112142811 _ 0000111111 _ ABCED11111 _ 1 _ MY59470233
  날짜/시간       프로젝트명    트레이ID     설비번호  설비명(SerialNum)
```
- 프로젝트명·트레이ID는 미설정인 경우가 많으므로 **데이터 추출에 사용 금지**
- **설비번호(1~5)** 만 셀 매핑에 사용

**내부 섹션 구조**:

```
[Cat AppInfo]
  SerialNum: MY59470230      ← 설비명 (파일명 설비명과 일치)

[Cat TestSetupGlobal]
  TestType: InsituLowSdm
  TestDuration: 15           ← 측정 시간 (분)
  Tint: 10                   ← 샘플링 간격 (초, 설정마다 다를 수 있음)
  StartTime: 2026-01-12 ...

[Cat TestSetupPerChan]       ← 채널별 설정 (헤더 행 포함)
  Channel | IsChanEnabled | ShowVmeas | ShowVtrace | ShowVtrace | limit | Rout | Ocp | Ovp | Uvp | Rwiring
  1       | TRUE          | ...                                                                 | 0.6049
  ...
  16      | TRUE          | ...                                                                 | 0.6805
  17      | FALSE         | ...                                                                 | 0      ← 미사용
  ...
  32      | FALSE         | ...                                                                 | 0

[Cat Measurements]           ← 시계열 측정값
  t(초) | I1  | I2  | ... | I32 | V1     | V2     | ... | V32
  10    | 7.5E-08 | 2.6E-08 | ... | 3.5915 | 3.5923 | ...
  20    | ...
```

- **IsChanEnabled**: 설비 5번만 ch 17~32가 FALSE (미사용), 설비 1~4번은 ch 1~32 전부 TRUE
  → 파서에서 IsChanEnabled=TRUE 채널만 추출하면 설비별 활성 채널이 자동 처리됨
- Rwiring 단위: Ω
- 전류(I) 단위: A (µA 수준, 예: 7.5E-08 A)
- 전압(V) 단위: V

---

### TEMP_DATA 파일 구조

```
TIME | T(01) | T(02) | T(03) | ... | T(144)
10   | 25.37 | 25.40 | 25.51 | ... | ...
20   | 25.25 | 25.28 | 25.52 | ... | ...
...
```

- 시간 단위: 초
- 온도 단위: °C
- T(01)~T(144) 가 셀 번호 1~144에 **직접 매핑** (설비 번호 변환 불필요)
- 샘플링 간격: KSS와 동일 (Tint 설정값)

---

### 설비번호 ↔ 셀번호 ↔ KSS 채널 매핑

| 설비번호 (파일명) | KSS 활성 채널 | 셀 번호 | 셀 수 |
|---|---|---|---|
| 5 | ch 1 ~ 16 | 셀 1 ~ 16 | 16 |
| 4 | ch 1 ~ 32 | 셀 17 ~ 48 | 32 |
| 3 | ch 1 ~ 32 | 셀 49 ~ 80 | 32 |
| 2 | ch 1 ~ 32 | 셀 81 ~ 112 | 32 |
| 1 | ch 1 ~ 32 | 셀 113 ~ 144 | 32 |

셀 번호 계산식:
```python
# 설비번호별 셀 오프셋
DEVICE_OFFSET = {5: 0, 4: 16, 3: 48, 2: 80, 1: 112}
cell_no = DEVICE_OFFSET[device_no] + channel_no  # channel_no: 1-based
```

---

### 통합 데이터프레임 설계

측정 1회분을 파싱하면 다음 두 가지 DataFrame으로 통합:

#### (1) 셀 메타 DataFrame — `df_meta` (행: 셀, 144행)

| 컬럼 | 출처 | 설명 |
|---|---|---|
| cell_no | 매핑 | 셀 번호 1~144 |
| device_no | 파일명 | 설비 번호 1~5 |
| channel_no | KSS | 설비 내 채널 번호 |
| Rwiring | KSS TestSetupPerChan | 배선 저항 (Ω) |
| V_init | KSS Measurements | 측정 시작 시점 전압 (V) |
| T_init | TEMP_DATA | 측정 시작 시점 온도 (°C) |
| T_final | TEMP_DATA | 측정 종료 시점 온도 (°C) |
| delta_T | 계산 | T_final − T_init |
| I_Nmin | KSS Measurements | N분 시점 전류값 (A) — 판정 종속변수 후보 |
| slope_0_N | KSS Measurements | 0~N분 구간 전류 기울기 (A/s) — v2 종속변수 |
| layer | 계산 | 트레이 레이어 (1~6) |

#### (2) 시계열 DataFrame — `df_ts` (행: 시간×셀)

| 컬럼 | 출처 | 설명 |
|---|---|---|
| cell_no | 매핑 | 셀 번호 |
| t_sec | KSS/TEMP | 시간 (초) |
| current_A | KSS Measurements | 전류 (A) |
| voltage_V | KSS Measurements | 전압 (V) |
| temp_C | TEMP_DATA | 온도 (°C) |

---

### 폴더 구조 및 다중 트레이 운용

```
상위 폴더/
├── CFDD000001/        ← 트레이 1 (폴더명 = 트레이 ID, 없는 경우도 있음)
│   ├── 20260112..._1_MY594702xx.kss
│   ├── 20260112..._2_MY594702xx.kss
│   ├── ...            (KSS 파일 1~5개, 있는 것만)
│   └── 20260112..._TEMP_DATA.xlsx
├── CFDD000002/        ← 트레이 2
│   └── ...
└── ...
```

- 사용자가 상위 폴더를 선택하면 하위 트레이 폴더 목록 자동 감지
- **최대 4개 트레이 동시 선택** → 동시 분석
- 폴더명에서 트레이 ID 추출 (폴더명이 곧 트레이 ID, 없으면 폴더명 그대로 사용)

---

### KSS 파일 수 유연화 (1~5개)

KSS 파일이 반드시 5개일 필요 없음. 셀을 일부 설비 위치에만 넣으면 해당 설비 KSS만 생성됨.

**감지 로직**:
```python
# 폴더 내 KSS 파일에서 설비번호 추출
present_devices = {파일명에서 추출한 설비번호}  # 예: {4} or {1,2,3,4,5}

# 설비번호 → 유효 셀 범위
DEVICE_CELL_RANGE = {
    5: range(1, 17),     # 셀 1~16
    4: range(17, 49),    # 셀 17~48
    3: range(49, 81),    # 셀 49~80
    2: range(81, 113),   # 셀 81~112
    1: range(113, 145),  # 셀 113~144
}

valid_cells = [c for d in present_devices for c in DEVICE_CELL_RANGE[d]]
# 나머지 셀은 측정값 없음(NaN)으로 처리
```

예시:
| KSS 파일 | 유효 셀 | 나머지 셀 |
|---|---|---|
| 설비 1~5 전부 | 1~144 | 없음 |
| 설비 4만 | 17~48 | 1~16, 49~144 → NaN |
| 설비 2, 3만 | 49~112 | 나머지 → NaN |

---

### 파서 구현 순서

```
1. 폴더 스캔
   ├─ KSS 파일 목록 및 설비번호 자동 감지 (1~5개)
   └─ TEMP_DATA 파일 존재 여부 확인

2. KSS 파일 파싱 (감지된 파일만)
   ├─ 파일명에서 설비번호 추출
   ├─ [Cat TestSetupGlobal] → Tint, TestDuration 추출
   ├─ [Cat TestSetupPerChan] → 채널별 Rwiring (IsChanEnabled=TRUE만)
   └─ [Cat Measurements] → 시간별 I, V 배열

3. TEMP_DATA 파싱
   └─ 시간별 T(01)~T(144) 배열 (없으면 온도 컬럼 NaN)

4. 셀 번호 매핑
   └─ (설비번호, 채널번호) → 셀 번호 변환, 미측정 셀은 NaN

5. df_meta 생성
   └─ Rwiring, V_init, T_init, T_final, delta_T, I_Nmin, slope 계산

6. df_ts 생성
   └─ KSS 전류·전압 + TEMP 온도를 셀×시간으로 병합
```

---

## BT2152B + BT2155A 소프트웨어

- 채널별 OCV 매칭 자동화
- 자가방전 전류 기록 및 시각화
- Pass/Fail 판정 기준 설정
- 데이터 내보내기 (CSV)

---

## 용어 정리

| 용어 | 설명 |
|---|---|
| OCV | Open Circuit Voltage — 개방회로전압 |
| ISD | Self-Discharge Current — 자가방전 전류 |
| RSD | Self-Discharge Resistance = Vcell / ISD |
| TCV | Temperature Coefficient of Voltage [µV/K] |
| τ | 시간상수 = (Rout + Rser + Rwire) × Ceff |
| Rout | 출력 저항 — τ와 노이즈 모두에 영향 |
| Rwiring | 배선/접촉 저항 — 캘리브레이션 대상 |
| Eact | 활성화에너지 (= 0.94 eV) |
| SEI | Solid Electrolyte Interphase — 음극 표면 보호 피막 |
| Dendrite | 덴드라이트 — 금속이물 주변 수지상 리튬 결정 → 단락 유발 |
| dOCV | Delta OCV — 일정 기간 OCV 강하량 (3일 기준) |
| SOC | State of Charge [%] |
| slope | 0~N분 구간 전류 기울기 ΔI/Δt [µA/min] |
| LOO-tray CV | Leave-One-Out by Tray Cross-Validation |
| VIF | Variance Inflation Factor — 다중공선성 진단 지표 (기준: <10) |
| WLS | Weighted Least Squares — 가중최소제곱 |

---

## 논문 출처

| 논문 | 파일명 | 핵심 기여 |
|---|---|---|
| Al-Zubaidi R-Smith et al. (2020, IEEE) | r-smith2020.pdf | SDM 원리, 등가회로 모델, 덴드라이트 시뮬레이션, Rout-τ 관계 |
| Al-Zubaidi R-Smith et al. (2023, Energy Reports) | 1-s2.0-S2352484723014610-main.pdf | TCV(SOC 30% 최적), Arrhenius(Eact=0.94eV), 온도별 ISD, Rout 비교 |

## 참고 URL

- [Keysight BT2152B 제품 페이지](https://www.keysight.com/us/en/product/BT2152B/self-discharge-analyzer.html)
- [자가방전 전류 측정 방법](https://www.keysight.com/us/en/use-cases/characterizing-self-discharge-current.html)
- [두 측정방법 비교 블로그](https://www.keysight.com/blogs/en/tech/bench/2020/06/03/shortening-lithium-ion-cell-manufacturing-time-a-comparative-study-of-two-methods-of-making-self-discharge-measurements)

---

## 불량 전류값 관련 주의사항

논문 기준 불량 셀의 ISD는 150 µA 이상으로 기술되어 있으나, **실제 공정에서는 그보다 훨씬 작은 편차로 나타나는 경우가 많음.**

- 150 µA는 심각한 단락의 극단적 예시
- 실제 저전압 불량은 양품 대비 수 µA~수십 µA 수준의 미묘한 이탈로 나타날 수 있음
- 절대값 기준 판정보다 **상대적 기준(표준편차 배수)** 이 더 중요한 이유
- 회귀 보정 후 z-score 표준화로 트레이 내 상대적 이탈도를 보는 방식이 핵심

---

## ══════════════════════════════════════════
## 분석 툴 설계안
## ══════════════════════════════════════════

### 목적 및 배포 방식

**목적**: 팀 구성원 누구나 (Python 비숙련자 포함) 사용 가능한 공통 SDM 분석 툴

**배포 방식**: Python + PySide6(Qt) + PyInstaller → **단일 .exe 파일**
- 설치 불필요, 더블클릭 실행
- 사내 공유 폴더에 .exe 하나만 배포

**기술 스택**:
- UI: PySide6 (Qt6)
- 데이터: Pandas
- 그래프: Matplotlib / Seaborn
- 회귀: Scikit-learn (OLS, Lasso), Statsmodels (Robust, VIF)
- 배포: PyInstaller (단일 실행 파일)

---

### 전체 구조 (7개 탭)

```
┌──────────────────────────────────────────────────────┐
│  SDM 분석 툴                          [설정] [도움말]  │
├───────┬──────┬──────┬──────┬──────┬──────┬──────────┤
│데이터  │원시   │히트맵│분석  │결과  │통합표│내보내기   │
│불러오기│탐색  │      │로직  │      │      │          │
└───────┴──────┴──────┴──────┴──────┴──────┴──────────┘
```

---

### 탭 1. 데이터 불러오기

- SDM 파일 선택 (CSV / Excel)
- 공정 데이터 파일 선택
- 트레이 선택 (단일 / 다중 배치)
- 컬럼 자동 매핑 + 수동 수정 가능
- 불러온 데이터 미리보기 테이블

---

### 탭 2. 원시 데이터 탐색

**시계열 그래프** (전체 오버레이 또는 셀 선택)
- SDM 전류 vs 시간
- 전압 vs 시간
- 온도 vs 시간

**기능**:
- 셀 클릭 → 해당 셀 강조 표시
- 상위 N% 전류 셀 자동 하이라이트
- 그래프 범위 조절

---

### 탭 3. 히트맵

트레이 12×12 배열로 시각화:

| 히트맵 | 내용 |
|---|---|
| SDM 전류 히트맵 | 위치별 전류값 |
| 온도 히트맵 | 위치별 온도 |
| Rwiring 히트맵 | 위치별 배선저항 |
| dOCV 히트맵 | 위치별 OCV 강하량 |
| 결과 등급 히트맵 | A(검정) / E(빨강) 위치 시각화 |

**기능**:
- 셀 클릭 → 해당 셀 시계열 그래프 팝업
- 컬러스케일 범위 수동 조절
- 이상치 임계값 라인 표시

---

### 탭 4. 분석 로직

**설정 패널**:
- 측정 시간 N 선택 (슬라이더: 5~15분)
- 모델 옵션 선택 (옵션 1~5 라디오 버튼)
- 종속변수 선택: 단일값(v1) 또는 기울기 slope(v2)
- 기준 조건 입력 (T=25°C, V=3590mV 등, 수정 가능)
- 이상치 제거 기준 설정 (Rwiring 임계값 등)

**실행 후 출력**:
- 회귀계수 테이블
- VIF 값 (10 이상 경고 표시)
- 모델 R², Adjusted R²
- 잔차 분포 그래프
- 보정 전/후 분포 비교 그래프
- 옵션 1~5 분리도 비교 표 (동시 실행 후 비교)

---

### 탭 5. 결과 및 판정

**핵심 그래프: 보정값 분포**
- A등급(양품) → 검은색 점
- E등급(불량) → 빨간색 점
- 기준선 → 드래그 또는 숫자 입력

**기준선 설정 방식** (동시 제공):
- 직접 숫자 입력
- "평균 + N×σ" 입력 (σ 배수 슬라이더)
- 기준선 이동 시 실시간 TP/FP/FN/TN 업데이트

**추가 그래프**:
- dOCV vs 보정값 산점도 (두 방법 일치성 검증)
- 분리도(N분) 커브 — 몇 분부터 판정 가능한지 시각화
- 옵션별 보정값 분포 비교

---

### 탭 6. 통합 결과 테이블

셀 1개 = 행 1개. 모든 정보가 열로 정리:

```
셀번호 | 레이어 | V_init | T_final | ΔT | Rwiring | OCV1~7 |
충전종료전압 | SDM_N분값 | 보정값 | z-score | 등급(A/E)
```

**기능**:
- 컬럼 정렬, 필터
- 등급 필터 (A만 / E만 / 전체)
- 행 클릭 → 해당 셀 시계열 그래프 팝업

---

### 탭 7. 내보내기

- 그래프 PNG / PDF 저장
- 통합 결과 테이블 Excel / CSV 저장
- 현재 설정(모델 옵션, 기준선, 기준조건) 저장/불러오기

---

### 추가 제안 기능

| 기능 | 설명 |
|---|---|
| 다중 트레이 트렌드 뷰 | 트레이 순서별 불량률 추이 그래프 — 공정 이상 조기 감지 |
| 모델 저장/재사용 | 학습된 회귀계수 저장 → 신규 트레이에 재적용 (매번 재학습 불필요) |
| 이상 채널 경보 패널 | Rwiring 이상·온도 이탈·측정 오류 채널 목록화 |
| 보고서 자동 생성 | 트레이별 요약(불량률, 분리도, 사용 옵션)을 PDF로 자동 출력 |
| 셀 이력 조회 | 셀 번호 입력 → 동일 위치 셀의 트레이 간 비교 |

---

### 기능 체크리스트

| 기능 | 합의 | 추가 제안 |
|---|---|---|
| 시계열 그래프 (전류/전압/온도) | ✅ | |
| 히트맵 4종 + 등급 히트맵 | ✅ | |
| 로직 5가지 옵션 + v2(slope) | ✅ | |
| A/E 등급 시각화 (검정/빨강) | ✅ | |
| 기준선 (숫자 + 표준편차 배수) | ✅ | |
| 통합 결과 테이블 | ✅ | |
| 내보내기 (Excel/PNG/PDF) | ✅ | |
| VIF 진단 + 경고 | ✅ | |
| 분리도(N분) 커브 | ✅ | |
| 옵션 비교 표 | ✅ | |
| 다중 트레이 트렌드 | | ✅ |
| 모델 저장/재사용 | | ✅ |
| 이상 채널 경보 | | ✅ |
| 보고서 자동 생성 | | ✅ |

---

### 구현 시 확인 필요 사항

1. **BT2152B 출력 파일 형태** — CSV/Excel 컬럼 구조 샘플 필요
2. **공정 데이터 파일 형태** — OCV1~7, 충전종료전압 컬럼 구조
3. **MVP 범위** — 전체 동시 구현 vs. 핵심 기능(히트맵+시계열+로직+판정) 먼저
